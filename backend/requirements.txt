# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Settings & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
sqlalchemy==2.0.35
asyncpg==0.29.0          # async PostgreSQL driver (used at runtime)
psycopg2-binary==2.9.9   # sync driver (used by Alembic migrations)
alembic==1.13.3

# --- Auth (Module 6) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1              # pinned: passlib 1.7.4 is incompatible with bcrypt>=4.1
email-validator==2.2.0     # backs Pydantic's EmailStr, used by signup/login schemas
python-multipart==0.0.9    # required for file uploads (resume PDFs/DOCX) and OAuth2 login form

# --- Caching (Redis, optional — REDIS_ENABLED toggles it; see app/core/cache.py) ---
redis==5.0.8

# --- External API client (GitHub Analysis module) ---
httpx==0.27.2

# --- Resume text extraction (Resume Analyzer module) ---
pdfplumber==0.11.4        # PDF text extraction
python-docx==1.1.2        # DOCX text extraction

# --- Utilities ---
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1              # mocks httpx requests/responses (used by GitHub Analysis tests)
